Fetch a list of saved schemas with optional filters.
---
tags: ["schemas"]
parameters:
- name: show_source
  in: query
  required: false
  description: Whether to show the JSON of each schema.
  schema: {type: boolean}
responses:
  200:
    description: "View data."
