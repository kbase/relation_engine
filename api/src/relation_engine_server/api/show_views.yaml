List out all the available views, optionally showing the parameters and AQL source query.
---
tags: ["views"]
parameters:
- name: show_source
  in: query
  description: Whether to show the AQL source code for each view.
  schema: {type: boolean}
responses:
  200:
    description: "Array of views."
