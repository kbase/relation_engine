Execute a view as a query against the database.
---
tags: ["views"]
parameters:
- name: view_name
  in: path
  required: true
  description: Name of the view for running the query.
  schema: {type: string}
requestBody:
  description: Arguments for the query.
  content:
    application/json:
      required: true
      schema: 
        type: object
        required: true
responses:
  200:
    description: Result data.
