name: ModelSEED_reaction_merge
type: edge
schema:
  "$schema": "http://json-schema.org/draft-07/schema#"
  type: object
  additionalProperties: true
  required: [_from, _to]
  description: |
    Merge obsolete records
  properties:
    _from:
      type: string
      description: The obsolete reaction record
    _to:
      type: string
      description: The extant reaction record
    id:
      type: string
    from:
      type: string
    to:
      type: string
