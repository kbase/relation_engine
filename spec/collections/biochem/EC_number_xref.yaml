name: EC_number_xref
type: edge
schema:
  "$schema": "http://json-schema.org/draft-07/schema#"
  type: object
  additionalProperties: true
  required: [_from, _to]
  description: |
    Cross references from EC numbers to external databases (namely, KEGG orthology)
  properties:
    _from:
      type: string
      description: The EC number
    _to:
      type: string
      description: The external database record
