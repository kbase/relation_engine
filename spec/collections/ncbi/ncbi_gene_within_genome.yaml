name: ncbi_gene_within_genome
type: edge
schema:
  "$schema": http://json-schema.org/draft-07/schema#
  type: object
  required: [_from, _to]
  properties:
    _from:
      type: string
      description: The ncbi_gene that is part of a genome.
    _to:
      type: string
      description: The ncbi_genome that contains a gene.
