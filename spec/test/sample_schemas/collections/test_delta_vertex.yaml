name: test_delta_vertex
type: vertex
delta: true
schema:
  "$schema": "http://json-schema.org/draft-07/schema#"
  type: object
  required: [id]
  description: An example vertex schema for testing
  properties:
    id: {type: string}
    quality: {type: string}