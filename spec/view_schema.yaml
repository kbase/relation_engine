name: view_schema
type: object
required: ['name', 'type']
properties:
  name:
    type: string
    title: View name
    format: regex
    pattern: ^\w+$
  type:
    type: string
    enum: ['arangosearch']
