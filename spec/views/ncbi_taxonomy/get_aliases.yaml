params:
  type: object
  required: [taxon_key]
  properties:
    taxon_key: {type: string}
query: |
  WITH ncbi_child_of_taxon, ncbi_taxon
  let taxon_key = @taxon_key
  for t in ncbi_taxon
    filter t._key == taxon_key
    return t.aliases
