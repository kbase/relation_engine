{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["_from", "_to"],
  "description": "The _from entity is exactly identical to the _to entity.",
  "properties": {
    "_from": { "type": "string" },
    "_to": { "type": "string" }
  }
}

