{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["_from", "_to"],
  "description": "A generic similarity association.",
  "additionalProperties": true,
  "properties": {
    "_from": {
      "type": "string",
      "description": "The ID of a vertex"
    },
    "_to": {
      "type": "string",
      "description": "The ID of a vertex"
    }
  }
}

