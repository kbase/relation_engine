{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "required": ["_from", "_to"],
  "properties": {
    "_from": {
      "type": "string",
      "description": "The ID of the document that contains something else."
    },
    "_to": {
      "type": "string",
      "description": "The ID of the document that is contained by something else."
    }
  }
}
