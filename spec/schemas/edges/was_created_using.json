{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "required": ["_from", "_to"],
  "description": "The _from entity was created using the _to entity as config or a tool. Eg: an assembly is created *using* spades, or homology data was created *using* mash sketches.",
  "properties": {
    "_from": {
      "type": "string",
      "description": "The entity that was created."
    },
    "_to": {
      "type": "string",
      "description": "The tool, configuration, or parameter that took part in the creation of _to."
    }
  }
}

